1. Explica la diferencia entre los métodos start() y run() en un hilo.
Pon un ejemplo breve donde se vea la diferencia.
start, ejecuta los hilos de forma secuencial
run, ejecuta los hilos de forma concurrente

2. ¿Qué es una condición de carrera?
Describe qué la provoca y menciona una técnica para evitarla.
Una condición de carrera, es la que indica cuando un hilo empieza y acaba su trabajo.
Provoca que dos hilos puedan acceder al mismo recurso sin problemas.
Lo mejor para evitarlo es usar la sincronización

3. ¿Qué es el modelo Productor–Consumidor?
Indica qué problema resuelve y en qué casos se utiliza.
Es exactamente igual que el modelo cliente servidor
Resuelve el problema de que un consumidor necesita un servicio y este productor se encarga de suministrárselo. 

4. Observa el siguiente código y responde:

Thread t = new Thread(() -> {
    for (int i = 0; i < 5; i++) {
        System.out.println("A");
    }
});

t.start();

System.out.println(t.isAlive());

a) ¿Qué puede imprimir isAlive()?
Nos indica si el proceso esta ejecutándose o no
b) ¿Por qué el resultado puede variar de una ejecución a otra?
Porque al usar start, esto hace que start inicie el hilo T y luego el principal, ya que esto ejecuta hilos de forma secuencial.

5. ¿Para qué sirve el método yield()?
Explica por qué no garantiza orden de ejecución entre hilos.
Sirve para que el procesador permita a otros hilos que se ejecuten.
No te da garantiza el orden porque lo único que hace es darle una pista al planificador como ayudarlo para elegir.

6. ¿Qué hace el método interrupt() en un hilo?
Indica cómo debería reaccionar un hilo bien programado cuando recibe una interrupción.
Esto hace como que le envia un aviso al hilo de que se ha interrumpido.
Deberia mostrarnos su estado de interrupción.

7. ¿Qué significa que un método sea synchronized?
Indica cómo afecta a la ejecución cuando varios hilos intentan acceder al mismo recurso.
Hace que los hilos se sincronizen y asi otros hilos que quieran entrar en el proceso se esperan

8. Explica la diferencia entre sleep() e interrupt().
Indica qué ocurre cuando interrumpimos un hilo que está durmiendo.
Sleep pausa el hilo actual.
Interrupt puede ser el hilo interrumpido mientras espera.
Cuando un hilo esta durmiendo y lo interrupimos, se cancela solo.

9. Dado el siguiente código, indica si produce o no una condición de carrera y por qué:

class Contador {
    public int valor = 0;
}

Contador c = new Contador();

Thread t1 = new Thread(() -> { for(int i = 0; i < 1000; i++) c.valor++; });
Thread t2 = new Thread(() -> { for(int i = 0; i < 1000; i++) c.valor++; });

t1.start();
t2.start();

Si se produce una condición de carrera, porque la variable c se modifica por los hilos que son t1 y t2

10. Describe qué es la prioridad de un hilo (setPriority()) y cómo afecta realmente a la ejecución.
Sirve para decirle al planificador del sistema cuales son los hilos mas importantes, unos mas que otros

¿Es una garantía de orden? ¿Por qué puede comportarse de forma diferente según el sistema operativo?
No te garantizan un orden porque depende el sistema operativo, cada sistema operativo planifica de una forma por eso cada uno es distinto.